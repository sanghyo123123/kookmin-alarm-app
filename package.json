{
  "name": "kookmin_alarm_app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "flutter build web",
    "start": "npm run build",
    "clean": "flutter clean",
    "test": "flutter test"
  },
  "devDependencies": {
    "rimraf": "^4.1.0",
    "rollup-plugin-terser": "^11.0.1"
  }
}
