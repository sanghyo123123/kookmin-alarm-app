name: kookmin_alarm_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.1.2
  intl: ^0.18.1
  flutter_local_notifications: ^13.0.0
  timezone: ^0.9.1


flutter:
  uses-material-design: true

  assets:
    - assets/busTimetable.json

